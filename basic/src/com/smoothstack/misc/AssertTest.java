package com.smoothstack.misc;

/**
 * asserts are enabled thru VM option in Java, and by default they are turned off.
 */
public class AssertTest {
    public AssertTest() {
        assert(1 == 1);
    }
}
